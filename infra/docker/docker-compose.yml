version: '3'
services:
  dev:
    container_name: dev
    image: jaepyo99/fitdo
    env_file:
      - ../../.env
    expose:
      - 8080
    ports:
      - 8080:8080
    tty: true
    environment:
      - TZ=Asia/Seoul
