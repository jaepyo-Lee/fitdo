dependencies {
    // jwt(fitdo-service)
    api 'io.jsonwebtoken:jjwt-api:0.11.5'
    api 'io.jsonwebtoken:jjwt-impl:0.11.5' // 전이 가능하도록 변경
    api 'io.jsonwebtoken:jjwt-jackson:0.11.5' // 전이 가능하도록 변경
}

tasks.named('test') {
    useJUnitPlatform()
}
bootJar{
    enabled=false
}
jar{
    enabled=true
}